# Cloudflare Pages Configuration
# This file tells Cloudflare Pages how to build your frontend

# Build settings
[build]
command = "npm install && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "18"
VITE_API_URL = "https://friendlyhealthy.tejasborade9594.workers.dev/api/v1"
