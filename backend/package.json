{
  "name": "friendlyhealthy-backend",
  "version": "1.0.0",
  "description": "Healthcare Management Platform API",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn app.main:app --reload",
    "worker": "celery -A app.services.celery_tasks.celery_app worker --loglevel=info"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
